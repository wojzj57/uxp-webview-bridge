# `PreferencesType`

Writable remote fields: `showTextFeatures`, `showEnglishFontNames`, `smartQuotes`.

Reads are async; writes queue. `showTextFeatures` uses the Photoshop type-interface feature constant.
