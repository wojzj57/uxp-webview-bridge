# `PreferencesHistory`

Writable remote fields: `createFirstSnapshot`, `nonLinearHistory`, `numberOfHistoryStates`, `useHistoryLog`, `editLogItems`, `saveLogItems`.

Reads are async; writes queue. Use Photoshop constants for log-item modes. Changing these affects host-wide Photoshop behavior, so make only product-authorized changes.
