# `Guides`

Read-only array-like snapshot from `document.guides`, with `parent`.

- `add(direction, coordinate) -> PsGuide`
- `removeAll()`

Re-read the document property after adding/deleting guides.
