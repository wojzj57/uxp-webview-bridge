# `GrayColor`

WebView-local value class: `new photoshop.GrayColor({ gray? })`.

`gray` is a synchronous writable number from 0 through 100; invalid/non-finite values throw `RangeError`. Default is zero. `typename` is `GrayColor`. No RPC or disposal.
