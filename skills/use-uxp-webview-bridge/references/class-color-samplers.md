# `ColorSamplers`

Read-only array-like snapshot from `document.colorSamplers`, with `parent`.

- `add({ x, y }) -> PsColorSampler`
- `removeAll()`

Re-read after changes; the existing snapshot does not grow or shrink.
