# `PreferencesGuidesGridsAndSlices`

Writable remote fields: `guideStyle`, `gridStyle`, `gridSubDivisions`, `showSliceNumber`.

Reads are async; writes queue. Use Photoshop constant values for line styles and `batchSet` for coordinated preference changes.
