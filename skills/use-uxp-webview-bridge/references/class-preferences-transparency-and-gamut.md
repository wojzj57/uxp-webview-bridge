# `PreferencesTransparencyAndGamut`

Writable remote fields: `gridSize`, `gamutWarningOpacity`.

Reads are async; writes queue. Use a Photoshop grid-size constant and validate opacity for the target host.
