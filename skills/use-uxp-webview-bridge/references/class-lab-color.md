# `LabColor`

WebView-local value class: `new photoshop.LabColor({ l?, a?, b? })`.

Synchronous ranges: `l` 0-100, `a` and `b` -128-127. Defaults: 100, 0, 0. Invalid/non-finite values throw `RangeError`. `typename` is `LabColor`. No RPC or disposal.
