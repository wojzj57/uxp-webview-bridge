# `PreferencesNotifications`

Writable remote boolean fields: `quietMode`, `showFeatureOnboarding`, `showToolTips`, `showWhatsNew`, `useRichToolTips`.

Reads are async; assignments queue. These are host-wide user preferences; do not change them incidentally.
