# `PsCountItem`

Remote Photoshop count marker.

Read-only async properties: `itemIndex`, `groupIndex`, `typename`, `parent`, `position`. Methods: `move(position)`, `remove`, `batchGet`, empty `batchSet`, `dispose`.

Use `CountItems` group operations for grouping, visibility, sizing, label, and color changes.
