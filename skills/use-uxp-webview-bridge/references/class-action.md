# `Action`

Remote Photoshop action.

Read-only async properties: `typename`, `id`, `index`, `parent`. Writable queued: `name`. Methods: `delete`, `duplicate`, `play`, `batchGet`, `batchSet`.

This DOM object is distinct from `photoshop.action` descriptors and notification APIs. Re-read the parent action tree after mutation.
