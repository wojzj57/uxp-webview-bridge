# `PreferencesInterface`

Writable remote fields: `dynamicColorSliders`, `textFontSize`, `colorChannelsInColor`.

Reads are async; writes queue. Use Photoshop constants for `textFontSize` and `batchSet` for grouped updates.
