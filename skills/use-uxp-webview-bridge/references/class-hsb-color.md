# `HSBColor`

WebView-local value class: `new photoshop.HSBColor({ hue?, saturation?, brightness? })`.

Synchronous ranges: hue 0-360, saturation 0-100, brightness 0-100. Defaults: 360, 0, 100. Invalid/non-finite values throw `RangeError`. `typename` is `HSBColor`. No RPC or disposal.
