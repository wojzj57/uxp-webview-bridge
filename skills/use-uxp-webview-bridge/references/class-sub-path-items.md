# `SubPathItems`

Read-only array-like snapshot from `await pathItem.subPathItems`, with `parent` path item. It has no mutating collection methods. Re-read the parent property when the underlying path changes.
