# `PreferencesTools`

Writable remote boolean fields: `showToolTips`, `useShiftKeyForToolSwitch`, `keyboardZoomResizesWindows`.

Reads are async; writes queue. Use `batchGet` / `batchSet` for grouped operations.
