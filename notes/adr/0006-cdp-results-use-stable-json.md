# CDP Results Use Stable JSON

CDP cases and suites report stable JSON results so local runs, CI, and diagnostics can consume the same output. Case results include identity, status, duration, optional result data, normalized error fields, and diagnostics; suite results aggregate those case results with suite-level status and duration.
