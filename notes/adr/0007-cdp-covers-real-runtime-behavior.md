# CDP Covers Real Runtime Behavior

UXP CDP tests cover behavior that requires a real UXP plugin, WebView message bridge, or Photoshop/UXP host API. Static structure, type exports, protocol normalization, registry dispatch, capability matrices, timeout behavior, and mockable error mapping belong in static or contract tests so the CDP gate stays focused on facts that faster tests cannot prove.
