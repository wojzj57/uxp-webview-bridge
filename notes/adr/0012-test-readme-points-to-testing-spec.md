# Test README Points to Testing Spec

The test directory keeps `README.md` as a quick-start for running the UXP fixture, while `TESTING.md` owns the testing standards and CDP suite rules. This keeps day-to-day commands easy to find without burying the normative test design in setup instructions.
