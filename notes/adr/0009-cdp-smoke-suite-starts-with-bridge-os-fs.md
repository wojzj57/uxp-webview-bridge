# CDP Smoke Suite Starts with Bridge, OS, and FS

The first CDP smoke suite covers the automation channel, public API loading, bridge connection, one real OS adapter call, one plugin-data filesystem roundtrip, and remote error shape. Photoshop cases start in a separate suite because they require document state and richer fixture setup beyond basic bridge viability.
